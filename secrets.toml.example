# Rename this file to secrets.toml (locally) and NEVER commit the real one to GitHub.
# On Streamlit Community Cloud, paste this into: App settings -> Secrets

OPENWEATHER_API_KEY = "your_openweathermap_api_key_here"
